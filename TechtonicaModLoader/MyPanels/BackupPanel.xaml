<UserControl x:Class="TechtonicaModLoader.MyPanels.BackupPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TechtonicaModLoader.MyPanels"
             xmlns:MyControls="clr-namespace:TechtonicaModLoader.MyControls"
             mc:Ignorable="d" 
             Height="50" d:DesignWidth="600">
    <Grid>
        <Border Background="{DynamicResource backgroundBrush}" CornerRadius="{DynamicResource borderCorners}">
            <Grid>
                <TextBlock x:Name="nameLabel" Text="Name" FontSize="16" Foreground="{DynamicResource textBrush}"
                           HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <MyControls:MyButton ButtonText="Restore" FontSize="16" Width="120" Margin="0,0,5,0" LeftClicked="OnRestoreClicked"/>
                    <MyControls:MyButton ButtonText="Delete" FontSize="16" Width="120" Margin="0,0,5,0" LeftClicked="OnDeleteClicked"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
